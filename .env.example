# Optional authenticated AI Coach. Keep real values in .env or Netlify only.
# Never add VITE_ to OPENAI_API_KEY: that would expose it to the browser.
COACH_ENABLED=false
OPENAI_API_KEY=
COACH_OWNER_EMAIL=
COACH_CONTACT_EMAIL=bridge@markoconnor.ai

# The anonymous browser trial is retired. This stale value cannot re-enable it.
COACH_TRIAL_ENABLED=false
